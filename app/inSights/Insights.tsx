import React from 'react'
import {Text, SafeAreaView, ScrollView} from 'react-native'

const Insights = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Insights</Text>
      </ScrollView>
    </SafeAreaView>
  )
}

export default Insights